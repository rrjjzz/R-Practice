## Code to compute solution to quadratic equation of the form ax^2 + bx + x
## Define variables
a <- 4
b <- 3
c <- -2

## Now compute solutions
solution_1 <- (-b + sqrt(b^2 - 4*a*c)) / ( 2*a )
solution_2 <- (-b - sqrt(b^2 - 4*a*c)) / ( 2*a )

log(exp(1))
log(8,2)
